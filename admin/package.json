{
  "name": "@angulio/admin",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json && npm run bundle",
    "bundle": "esbuild src/index.ts --bundle --outfile=public/bundle.js --format=esm --sourcemap"
  },
  "dependencies": {
    "@angulio/shared": "*"
  }
}
